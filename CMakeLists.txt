project( cnn3 )
find_package( OpenCV REQUIRED )
aux_source_directory( src/. src_list )
add_executable( cnn3 ${src_list} )
target_link_libraries (cnn3 ${lstdc++})
target_link_libraries (cnn3 ${OpenCV_LIBS})